src:
  outputs:
    dev:
      type: duckdb
      path: ./dev.duckdb
      schema: raw
      extensions:
        - httpfs
        - parquet
      threads: 1

    prod:
      type: duckdb
      path: prod.duckdb
      threads: 4

  target: dev